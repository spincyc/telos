---
schema_version: 1
event_uuid: "1ada1eb4-0d04-45b6-ad0e-6cfe5561b523"
event_type: "checkpoint"
scope: "task"
task_ids: ["cfcab9ef-1f05-46d5-967e-a5a4bb43d923"]
agent_instance_uuid: "053c175f-c27d-434e-8f57-f013909ce432"
created_at: "2026-07-28T11:42:05Z"
---

# Prove disk-first Windows boot and bypass OOBE

The persistent live run completed the first Windows Setup phase with about
16 GB allocated. Its first reset loaded and started Windows Boot Manager from
the NVMe ESP instead of returning to PXE. Native Windows installation
progressed, reset again through Windows Boot Manager, and stopped at the
country/region OOBE page. Serial evidence contains exactly one PXE firmware
start and two Windows Boot Manager starts. The runner was stopped at that
stable prompt; cleanup removed all QEMU and fabric processes.

The existing en-US answer values select the desired locale but do not suppress
the machine and user OOBE flows. The private answer now explicitly skips both,
in addition to the previously added one-time native logon, power assertions,
and clean shutdown. Focused contract and lifecycle tests pass. The installed
OOBE evidence disk will not be reused; prepare a fresh bundle and run the full
native assertion lifecycle.
