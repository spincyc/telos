---
schema_version: 1
event_uuid: "9967ea95-ea1f-4d6b-97b6-e86bdaf92219"
event_type: "progress"
scope: "task"
task_ids: ["cfcab9ef-1f05-46d5-967e-a5a4bb43d923"]
agent_instance_uuid: "053c175f-c27d-434e-8f57-f013909ce432"
created_at: "2026-07-28T12:54:52Z"
---

# Prove native Windows shutdown and diagnose the false-negative receipt

Bundle `run-20260728T114233Z-afecdf7cc9d0` reached native Windows and emitted:

- `TELOS WINDOWS NATIVE READY`;
- `Current Edition : Professional`;
- `HiberbootEnabled REG_DWORD 0x0`; and
- the `powercfg /a` response before shutdown.

Firmware logged one PXE load line and one PXE start line. The lifecycle
validator counted both occurrences of the boot-option name and therefore
raised a false failure after the guest had shut down. It now counts only
`BdsDxe: starting` lines, its regression fixture includes the normal loading
line, and machine-readable failure receipts include the redacted exception
message. All 74 Windows-focused tests pass.

A read-only sparse raw conversion of the evidence disk confirms a 256 GiB GPT
disk with a 1 GiB ESP, 16 MiB MSR, 181.7 GiB Windows partition, preserved
71.2 GiB gap, and 2 GiB recovery partition. The ESP is readable as `SYSTEM`;
the Windows partition probes as NTFS labeled `Windows`. Its `$Volume`
information field reports flags `0x0000`, so the NTFS dirty-volume flag is
clear after native shutdown.

The immutable live `result.json` remains a truthful record of the runner
false-negative and was not rewritten.
