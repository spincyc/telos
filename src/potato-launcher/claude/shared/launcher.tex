% Shared macros for the potato launcher project.
%
% Same house style as the electricity curriculum: pictures of the actual parts
% first, cross-sections and graphs second. A launcher is a simple object and the
% whole of it can be drawn in one figure, so most sheets carry one.

\tikzset{
  pvc/.style={draw=telosink,line width=1.0pt},
  pvcfill/.style={fill=telosfill},
  pvcdetail/.style={draw=telosink,line width=0.35pt},
  pvchidden/.style={draw=telosmute,line width=0.35pt,dash pattern=on 2pt off 1.6pt},
  gas/.style={draw=telosmute,line width=0.4pt},
  lnlabel/.style={font=\fontsize{6.4}{7.4}\selectfont},
  lnsmall/.style={font=\fontsize{5.8}{6.8}\selectfont},
  lntag/.style={font=\fontsize{6.4}{7.4}\selectfont\itshape},
}

% ---------------------------------------------------------------------------
% The launcher in section. Chamber on the left with its cleanout and igniter,
% reducer, barrel to the right.
%   \launchersection{x}{y}{scale}{chamber length}{barrel length}
% ---------------------------------------------------------------------------
\newcommand{\launchersection}[5]{%
  \begin{scope}[shift={(#1,#2)},scale=#3]
    % chamber body
    \draw[pvc,pvcfill] (0,0) rectangle (#4,1.5);
    \draw[pvcdetail] (0,0.14) -- (#4,0.14);
    \draw[pvcdetail] (0,1.36) -- (#4,1.36);
    % screw-on cleanout at the breech
    \draw[pvc,pvcfill] (-0.45,-0.12) rectangle (0,1.62);
    \foreach \y in {0.1,0.35,0.6,0.85,1.1,1.35} {
      \draw[pvcdetail] (-0.45,\y) -- (-0.30,\y+0.06);}
    % reducer
    \draw[pvc,pvcfill] (#4,0) -- (#4+0.7,0.55) -- (#4+0.7,0.95) -- (#4,1.5) -- cycle;
    % barrel
    \draw[pvc,pvcfill] (#4+0.7,0.55) rectangle (#4+0.7+#5,0.95);
    \draw[pvcdetail] (#4+0.7,0.66) -- (#4+0.7+#5,0.66);
    \draw[pvcdetail] (#4+0.7,0.84) -- (#4+0.7+#5,0.84);
    % igniter, through the chamber wall
    \draw[pvc] (#4*0.42,1.5) -- (#4*0.42,2.05);
    \draw[pvc,pvcfill] (#4*0.42-0.22,2.05) rectangle (#4*0.42+0.22,2.45);
    \fill[telosink] (#4*0.42-0.06,1.62) circle (0.05);
    \fill[telosink] (#4*0.42+0.06,1.62) circle (0.05);
    \draw[telos hair] (#4*0.42-0.03,1.68) -- (#4*0.42,1.58) -- (#4*0.42+0.03,1.68);
  \end{scope}}

% A potato seated in the muzzle end of a barrel.
\newcommand{\spud}[3]{%
  \begin{scope}[shift={(#1,#2)},scale=#3]
    \fill[telospaper] (0,0) .. controls (0.10,0.26) and (0.46,0.26) .. (0.56,0)
      .. controls (0.46,-0.26) and (0.10,-0.26) .. (0,0) -- cycle;
    \draw[pvc] (0,0) .. controls (0.10,0.26) and (0.46,0.26) .. (0.56,0)
      .. controls (0.46,-0.26) and (0.10,-0.26) .. (0,0) -- cycle;
    \foreach \p/\q in {0.16/0.10, 0.30/-0.08, 0.42/0.12} {
      \fill[telosmute] (\p,\q) circle (0.022);}
  \end{scope}}

% Dimension line with a label.
\newcommand{\lndim}[5]{%
  \draw[|<->|,pvcdetail] (#1,#2) -- (#3,#4);
  \node[lntag,fill=telospaper,inner sep=1.6pt] at ({(#1+#3)/2},{(#2+#4)/2}) {#5};}

% Callout with a hairline leader.
\newcommand{\lncall}[6]{%
  \draw[pvcdetail,telosmute] (#1,#2) -- (#3,#4);
  \fill[telosmute] (#1,#2) circle (0.035);
  \node[lnlabel,anchor=#5,align=left,inner sep=1.6pt] at (#3,#4) {#6};}

% ---------------------------------------------------------------------------
% Standing safety text, written once.
% ---------------------------------------------------------------------------
\newcommand{\launchernote}{%
  \begin{teloshazard}{This is a projectile weapon}
  A combustion launcher throws a half-pound potato several hundred feet at
  speeds that will kill a person. It is not a toy, it is not a firework, and the
  fact that it is made of plumbing parts changes nothing about what it does
  downrange. Every rule on the \textbf{Safety} sheet applies every time: adult
  present and in charge, muzzle discipline as strict as a rifle's, a confirmed
  clear range, eyes and ears on, and never a person, animal, building, road or
  anything you cannot afford to destroy in the direction it is pointing.
  \end{teloshazard}}

\newcommand{\pvcnote}{%
  \begin{telosframe}
  \textbf{Pressure-rated pipe only.} Schedule 40 \emph{pressure} PVC, printed
  with an NSF-PW rating and a pressure figure. Never DWV, never foam-core,
  never cellular-core, never thin-wall drain pipe --- those are not rated for
  internal pressure at all and they fail by shattering into fragments rather
  than splitting. This is the one specification in the whole project that is
  not negotiable.
  \end{telosframe}}

% Craft sheets: transferable shop skill, drawn in section.
\input{potato-launcher/claude/shared/craft.tex}
