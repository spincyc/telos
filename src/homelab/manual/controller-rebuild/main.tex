\input{common/preamble.tex}
\input{homelab/shared/homelab.tex}

\telosmeta{Homelab}{Controller Rebuild}
  {Bare metal to serving DHCP, assuming nothing else works}{2026-07-25}

\tikzset{
  rebuild pencil/.style={draw=black!76,line width=.42pt,line cap=round,line join=round},
  rebuild faint/.style={draw=black!32,line width=.25pt,line cap=round,line join=round},
  rebuild hatch/.style={pattern=north east lines,pattern color=black!24},
  rebuild arrow/.style={-{Stealth[length=1.8mm]},draw=black!72,line width=.48pt}
}
\newcommand{\rebuildcaption}[1]{\par\smallskip{\centering\footnotesize\itshape #1\par}}

\begin{document}
\telosmasthead[Standalone reconstruction manual]

This manual assumes the Controller is dead and that nothing it hosted is
available: no DNS, no PXE, no artifact service, no Git remote you can reach
through it. It assumes you have this sheet on paper, an existing workstation,
and the recorded inputs for the machine you are rebuilding. If any step here
requires something the Controller was providing, that step is a defect --- report
it rather than working around it.

\begin{gate}{Before you start, confirm you have all five}
\textbf{1.} This document, on paper or on a device that is not the Controller.
\textbf{2.} A working Arch workstation to act as the bootstrap host.
\textbf{3.} The installer image and its published SHA-256, or the ability to
build it from this repository.
\textbf{4.} The recorded instance values for this Controller: hostname, managed
subnet, Controller address, DHCP pool, and the managed interface's MAC address.
\textbf{5.} The LUKS2 recovery material, held off-host. Without it an existing
encrypted disk is unrecoverable, and that is by design.
\end{gate}

\section{The rebuild state machine}
Every arrow below has an observable boundary. Do not advance because elapsed
time or familiar-looking output makes the next state seem likely.

\begin{center}
\begin{tikzpicture}[node distance=7mm and 8mm,font=\scriptsize]
\node[role] (prepared) {prepared\\tests + digest};
\node[role,right=of prepared] (booted) {installer booted\\no disk writes};
\node[role,right=of booted] (authorized) {authorized\\serial matched};
\node[role,below=of authorized] (off) {installed\\powered off};
\node[role,left=of off] (active) {activated\\services atomic};
\node[role,left=of active] (accepted) {accepted\\record signed};
\draw[flow] (prepared)--node[hlabel,above]{verified image}(booted);
\draw[flow] (booted)--node[hlabel,above]{questions answered}(authorized);
\draw[flow] (authorized)--node[hlabel,right]{destructive write}(off);
\draw[flow] (off)--node[hlabel,above]{move while dark}(active);
\draw[flow] (active)--node[hlabel,above]{measure + converge}(accepted);
\draw[flow,dashed] (booted) to[bend right=35]
  node[hlabel,below]{mismatch: stop}(prepared);
\draw[flow,dashed] (active) to[bend left=26]
  node[hlabel,below]{conflict: services off}(off);
\end{tikzpicture}
\rebuildcaption{Only recorded evidence moves the rebuild forward.}
\end{center}

\section{Stage 0 --- Prepare the bootstrap host}
\governedby{ADR 0052}

The bootstrap host runs the same dnsmasq and nginx configuration the Controller
profile uses, generated by the same code, so the path you are about to exercise
is the path you have already tested.

\begin{shellblock}
git clone <this repository> telos \&\& cd telos/homelab
python3 -m unittest discover -s tests -t .
\end{shellblock}
\expectthis{\texttt{OK}. If the test suite fails, stop. The installer's
validation logic is what stands between an operator error and a wiped disk, and
it is not optional.}

Build or fetch the installer image and verify it:

\begin{shellblock}
sha256sum -c artifacts/manifest.sha256
\end{shellblock}
\expectthis{Every line reports \texttt{OK}. A mismatch means the artifact is not
the one that was tested --- do not proceed. \governedby{ADR 0043, ADR 0048}}
\failurebranch{Quarantine the mismatched image. Rebuild or reacquire it, then
record both the expected and observed SHA-256; never repair the manifest to
match an unexplained artifact.}

\askbefore{What exact image filename and SHA-256 will be entered in the
acceptance record? Which interface is connected to the bootstrap segment, and
which other interfaces are physically or administratively down?}

\proofpair{ip -br link}{Only the intended bootstrap interface is up on the
bench segment; record its permanent MAC.}
\proofpair{sha256sum artifacts/<image>}{The 64 hexadecimal digits exactly match
the separately obtained manifest value.}

\begin{gate}{If the network already has a DHCP server}
Run the bootstrap dnsmasq in \textbf{ProxyDHCP} mode so it offers only boot
options and never addresses. Two address-assigning DHCP authorities on one
layer-2 network is forbidden at every stage, including this one.
\governedby{ADR 0008}
\end{gate}

\askbefore{Is the segment isolated, or is an existing DHCP authority present?
Name the observation that proves the answer. If it is uncertain, use ProxyDHCP
or stop; uncertainty never authorizes address service.}

\begin{center}
\begin{tikzpicture}[x=1cm,y=1cm,font=\scriptsize]
  % Workstation, drawn as a three-quarter technical pencil study.
  \fill[black!3] (-5.55,-.48) -- (-3.30,-.48) -- (-3.05,-.27) -- (-5.31,-.27) -- cycle;
  \draw[rebuild pencil,rounded corners=1pt] (-5.65,.05) rectangle (-3.20,1.67);
  \draw[rebuild faint] (-5.54,.17) rectangle (-3.31,1.56);
  \draw[rebuild pencil] (-4.43,.05)--(-4.43,-.27);
  \draw[rebuild pencil] (-5.05,-.42)--(-3.81,-.42)--(-3.55,-.27)--(-5.29,-.27)--cycle;
  \foreach \y in {.40,.54,.68,.82,.96,1.10,1.24}
    \draw[rebuild faint] (-5.33,\y)--(-4.22,\y);
  \node[align=center] at (-4.43,1.08) {\textsc{bootstrap}\\workstation};
  % Switch with ports, feet, and faceplate detail.
  \fill[black!4] (-1.72,-.17) rectangle (1.48,.61);
  \draw[rebuild pencil] (-1.82,-.07) rectangle (1.38,.71);
  \draw[rebuild faint] (-1.70,.04) rectangle (1.26,.60);
  \foreach \x in {-1.48,-1.03,-.58,-.13,.32,.77}
    {\draw[rebuild pencil] (\x,.15) rectangle +( .31,.24);
     \draw[rebuild faint] (\x+.06,.15)--(\x+.06,.39);}
  \fill[black!55] (1.06,.25) circle (.035);
  \node[above] at (-.22,.76) {isolated switch};
  % Target tower with louvers, NIC and removable drive bay.
  \fill[black!3] (3.16,-.52) -- (5.45,-.52) -- (5.24,-.32) -- (3.16,-.32) -- cycle;
  \draw[rebuild pencil,rounded corners=1pt] (3.08,-.39) rectangle (5.18,1.83);
  \draw[rebuild faint] (3.21,-.25) rectangle (5.05,1.68);
  \draw[rebuild pencil] (3.42,1.24) rectangle (4.84,1.54);
  \draw[rebuild faint] (3.52,1.33)--(4.42,1.33);
  \foreach \y in {.03,.19,.35,.51,.67,.83}
    \draw[rebuild faint] (3.48,\y)--(4.72,\y);
  \draw[rebuild pencil] (4.46,-.18) rectangle (4.90,-.02);
  \fill[black!58] (3.38,-.10) circle (.045);
  \node[align=center] at (4.13,1.02) {\textsc{target}\\UEFI PXE};
  % Cable paths and small graphite shadows.
  \draw[rebuild pencil,double=black!18,double distance=.65pt]
    (-3.20,.46) .. controls (-2.65,.46) and (-2.43,.30) .. (-1.82,.30);
  \draw[rebuild pencil,double=black!18,double distance=.65pt]
    (1.38,.30) .. controls (2.11,.30) and (2.30,-.10) .. (3.08,-.10);
  \draw[rebuild arrow] (-2.68,.93)--(-1.92,.93)
    node[midway,above,align=center]{boot image\\DHCP/PXE};
  \draw[rebuild arrow] (1.54,.93)--(2.65,.93)
    node[midway,above]{network boot};
  \node[below,align=center] at (-.22,-.30)
    {one layer-2 segment\\no second address authority};
\end{tikzpicture}
\rebuildcaption{Bootstrap bench: verify the segment before the target requests a lease.}
\end{center}

\section{Stage 1 --- Network boot the target}
Set the target's firmware to UEFI network boot. The installer refuses a target
booted in legacy BIOS or CSM mode and stops before offering any destructive
action. \governedby{ADR 0019}

\expectthis{The Archiso environment reaches a shell and the installer starts.
It has made no change to any disk at this point and will not until Stage 3.}
\failurebranch{If PXE fails, keep the target in firmware or the read-only live
environment. Check link, lease/ProxyDHCP traffic, TFTP handoff, HTTP status and
image digest in that order. Do not change target disks while diagnosing boot.}

\proofpair{test -d /sys/firmware/efi}{Exit status zero proves UEFI boot; any
failure means reboot and disable legacy/CSM.}
\proofpair{lsblk -o NAME,SIZE,MODEL,SERIAL,TYPE}{Inventory only: record the
candidate disk tuple before the installer asks for authorization.}

\section{Stage 2 --- Answer, and read the summary}
\governedby{ADR 0004, ADR 0045, ADR 0050}

The installer collects, in order: the profile, the hostname, the target disk,
the managed interface, and --- when Controller network services are enabled --- the
four network inputs. It validates each as you enter it.

\begin{tabularx}{\linewidth}{@{}p{0.26\linewidth}L@{}}
\toprule
\textbf{Input} & \textbf{What is checked}\\
\midrule
Target disk & Confirmed by model, serial and size. Never by \texttt{/dev/sdX}, because that is not stable across boots. Disks under 64 GiB are refused. \governedby{ADR 0051}\\
Managed interface & Chosen from an enumerated list showing kernel name, permanent MAC, link state and speed. There is no default. The MAC becomes the identity and is pinned to \texttt{lan0}.\\
\texttt{managed\_ipv4\_cidr} & Parses as IPv4, and names the network address itself --- a CIDR with host bits set is rejected rather than silently normalised.\\
\texttt{controller\_ipv4\_address} & Usable unicast address inside the subnet, and \emph{outside} the DHCP pool.\\
\texttt{dhcp\_pool\_start/end} & Both usable and inside the subnet; start not greater than end.\\
\bottomrule
\end{tabularx}

Everything else is derived and never prompted for: netmask, network and
broadcast addresses, the client DNS server (the Controller), the suffix
(\texttt{home.arpa}), and the absence of a default-router option.

\begin{gate}{The preflight summary is the authorization boundary}
The summary states the exact disk that will be destroyed, by model and serial;
the profile and hostname; and the complete entered and derived network plan.
Read the disk serial aloud against the label on the drive. Physical access plus
this confirmation \emph{is} the authorization model --- there is no second
approval anywhere. Selecting a profile has authorized nothing.
\end{gate}

\askbefore{Does the physical drive label match the summary's model, serial and
size character for character? Is the managed-interface permanent MAC the one
in the instance record? Is the Controller address outside the printed pool?
If any answer is not an unqualified yes, which input will be corrected?}

\proofpair{lsblk -dn -o MODEL,SERIAL,SIZE <device>}{The live tuple matches both
the physical label and the installer summary; save the output with the record.}
\proofpair{ip -br link}{The selected permanent MAC belongs to the connected
managed interface and no interface was accepted by positional name alone.}

\begin{center}
\begin{tikzpicture}[x=1cm,y=1cm,font=\scriptsize]
  % Physical 3.5-inch drive, including mounting holes and connector block.
  \fill[black!4] (-5.55,-1.03) -- (-1.45,-1.03) -- (-1.17,-.77) --
    (-1.17,1.30) -- (-5.26,1.30) -- (-5.55,1.03) -- cycle;
  \draw[rebuild pencil] (-5.55,-1.03) -- (-1.45,-1.03) -- (-1.17,-.77) --
    (-1.17,1.30) -- (-5.26,1.30) -- (-5.55,1.03) -- cycle;
  \draw[rebuild faint] (-5.38,-.85) rectangle (-1.43,1.08);
  \foreach \p in {(-5.16,-.65),(-1.72,-.65),(-5.16,.84),(-1.72,.84)}
    {\draw[rebuild pencil] \p circle (.11);\draw[rebuild faint] \p circle (.055);}
  \draw[rebuild pencil] (-4.73,-.46) rectangle (-2.03,.68);
  \draw[rebuild faint] (-4.60,-.31)--(-2.18,-.31);
  \node[anchor=west] at (-4.54,.43) {\bfseries MODEL\quad AX4000};
  \node[anchor=west] at (-4.54,.13) {S/N\quad \ttfamily 7K3A-19F2};
  \node[anchor=west] at (-4.54,-.16) {4000 GB};
  \foreach \x in {-4.49,-4.39,...,-2.44}
    \draw[rebuild faint] (\x,-.39)--(\x,-.25);
  \node[below] at (-3.36,-1.10) {label on the physical drive};
  % Installer summary sheet.
  \fill[black!2] (.32,-1.03) rectangle (5.40,1.31);
  \draw[rebuild pencil] (.32,-1.03) rectangle (5.40,1.31);
  \draw[rebuild faint] (.50,-.84) rectangle (5.21,1.12);
  \node[anchor=west,font=\scriptsize\bfseries] at (.68,.86) {DESTRUCTIVE INSTALL SUMMARY};
  \draw[rebuild faint] (.68,.70)--(4.94,.70);
  \node[anchor=west] at (.68,.43) {model};
  \node[anchor=west,font=\ttfamily] at (2.05,.43) {AX4000};
  \node[anchor=west] at (.68,.12) {serial};
  \node[anchor=west,font=\ttfamily\bfseries] at (2.05,.12) {7K3A-19F2};
  \node[anchor=west] at (.68,-.19) {size};
  \node[anchor=west,font=\ttfamily] at (2.05,-.19) {4000 GB};
  \draw[rebuild pencil] (.69,-.70) rectangle (.89,-.50);
  \node[anchor=west] at (1.02,-.60) {I verified model, serial, and size};
  \draw[rebuild arrow,<->] (-.91,.11)--(.04,.11);
  \node[above,align=center] at (-.43,.20) {read aloud\\match exactly};
\end{tikzpicture}
\rebuildcaption{Authorization is a physical comparison, never a device-name guess.}
\end{center}

\section{Stage 3 --- Install}
After confirmation the installer partitions, encrypts, creates filesystems,
installs the base system with both kernels, writes systemd-boot and the UKIs,
configures \texttt{lan0} and the static address, installs \texttt{sshd} with your
public key, writes the manifest, syncs, and \textbf{powers off}. It does not
reboot. \governedby{ADR 0010}

\expectthis{A clean poweroff, not a reboot. If the machine reboots into the
installed system while still on the provisioning network, power it off before
it can start DHCP.}
\failurebranch{If installation stops before poweroff, photograph or capture the
last successful phase and error, leave the target off, and inspect the log from
the live environment. Never infer that an incomplete disk is bootable.}

\askbefore{Has the installer displayed its final sync completion and requested
no further input? Is the chassis visibly off before any cable is moved?}

\section{Stage 4 --- Relocate, then first boot}
Move the powered-off Controller to the isolated switch and power it on. Being
powered off during the move is the primary DHCP-conflict safety boundary; it is
physical, not a configuration flag.

\begin{center}
\begin{tikzpicture}[x=1cm,y=1cm,font=\scriptsize]
  % Source bench, separation boundary, and destination segment.
  \draw[rebuild faint,dashed] (0,-1.08)--(0,1.55);
  \node[above] at (-3.72,1.48) {provisioning bench};
  \node[above] at (3.52,1.48) {isolated managed segment};
  \draw[rebuild pencil] (-5.45,-.50) rectangle (-3.65,.94);
  \draw[rebuild faint] (-5.31,-.36) rectangle (-3.80,.80);
  \foreach \y in {-.10,.08,.26,.44}\draw[rebuild faint](-5.07,\y)--(-4.10,\y);
  \node at (-4.55,.65) {Controller};
  % Prominent physical power state.
  \draw[rebuild pencil] (-3.23,.49) circle (.23);
  \draw[rebuild pencil] (-3.23,.79)--(-3.23,.48);
  \node[below,align=center] at (-3.23,.14) {\bfseries POWER OFF\\cables removed};
  \draw[rebuild arrow] (-2.55,-.32) .. controls (-1.68,-.76) and
    (1.27,-.76) .. (2.10,-.31);
  \node[below] at (0,-.74) {carry only while dark};
  % Destination switch, target, and clients.
  \draw[rebuild pencil] (2.22,-.49) rectangle (4.31,.13);
  \foreach \x in {2.43,2.79,3.15,3.51,3.87}
    \draw[rebuild pencil] (\x,-.30) rectangle +( .22,.17);
  \node[above] at (3.27,.17) {isolated switch};
  \draw[rebuild pencil] (4.77,-.63) rectangle (5.69,.92);
  \draw[rebuild faint] (4.90,-.50) rectangle (5.56,.78);
  \node[rotate=90] at (5.23,.13) {first boot};
  \draw[rebuild pencil,double=black!18,double distance=.6pt]
    (4.31,-.20)--(4.77,-.20);
  \draw[rebuild pencil] (2.25,.74) rectangle (3.15,1.22);
  \draw[rebuild pencil] (3.50,.74) rectangle (4.40,1.22);
  \draw[rebuild pencil] (2.70,.74)--(2.70,.55)--(2.78,.55);
  \draw[rebuild pencil] (3.95,.74)--(3.95,.55)--(3.87,.55);
  \draw[rebuild pencil,double=black!18,double distance=.6pt]
    (2.78,.55)--(2.78,.13);
  \draw[rebuild pencil,double=black!18,double distance=.6pt]
    (3.87,.55)--(3.87,.13);
  \node[above] at (3.33,1.24) {test clients};
  \node[align=center] at (0,.61) {\bfseries DHCP SAFETY\\BOUNDARY};
\end{tikzpicture}
\rebuildcaption{Relocation sequence: off, disconnect, move, connect, then boot.}
\end{center}

Under \texttt{development-proof} mode you will be asked for the LUKS2
passphrase at every boot. That is expected --- Milestone A enrolls no TPM token.
\governedby{ADR 0043}

First-boot activation verifies, before starting anything: that \texttt{lan0}
exists, carries the expected MAC and has carrier; that the static address is
configured; and that no other DHCP authority answers on the segment.

\expectthis{\texttt{systemctl is-active dnsmasq nginx} reports \texttt{active}
for both. If activation failed, both are stopped and \texttt{journalctl -u
homelab-first-boot} states which check failed. A partial start is a failure.
\governedby{ADR 0009, ADR 0012}}
\failurebranch{Keep both services stopped. Capture
\texttt{journalctl -u homelab-first-boot -b} and the competing DHCP server's
MAC, if reported. Remove the conflict or correct the interface/address record;
do not start either daemon by hand.}

\askbefore{Is the Controller now cabled only to the intended isolated segment?
What observation shows that no other DHCP authority answered before activation?
Are both services stopped if any preflight check failed?}

\section{Stage 5 --- Prove it}
From a separate client on the isolated switch:

\begin{shellblock}
ip addr show          \# a lease inside the configured pool
ip route show         \# no default route
dig +short \placeholder{hostname}.home.arpa @\placeholder{controller-address}
\end{shellblock}

\proofpair{ip -4 -o addr show scope global}{Exactly one managed address, inside
the configured pool; record address, prefix and client MAC.}
\proofpair{ip -4 route show default}{No output and exit status zero. Any default
route is an acceptance failure.}
\proofpair{dig +short <host>.home.arpa @<controller>}{Exactly the recorded
Controller address, with no extra answer.}
\proofpair{sha256sum /tmp/ipxe}{After fetching from the printed boot URL, HTTP
succeeded and the observed digest matches the artifact manifest.}

\begin{telosfacts}[0.30]
\telosfact{Lease}{Inside the pool, and never the Controller's own address.}
\telosfact{No default route}{The client must have none. A default route here means dnsmasq advertised a router that does not exist, and every client on this segment is black-holed. \governedby{ADR 0011}}
\telosfact{DNS}{Returns the Controller's address.}
\telosfact{PXE}{A second target network boots from this Controller.}
\telosfact{Idempotence}{A second Ansible converge run reports no changes.}
\end{telosfacts}

\section{Stage 6 --- Converge}
\governedby{ADR 0053}

Everything beyond the base install is Ansible over SSH from this repository.
Run it in check mode first and read the diff.

\begin{shellblock}
ansible-playbook -i \placeholder{inventory} controller.yml --check --diff
ansible-playbook -i \placeholder{inventory} controller.yml
ansible-playbook -i \placeholder{inventory} controller.yml --check
\end{shellblock}
\expectthis{The third run reports zero changed tasks. A role that is not
idempotent is a role that will surprise you during an outage.}
\failurebranch{Do not repeat the mutating run until it happens to look clean.
Save the first and check-mode outputs, identify the task that remains changed
or failed, correct its declared state, then restart with check mode.}

\askbefore{Does check mode propose only intended changes? Can each destructive
or service-restarting task be tied to the reviewed commit? Is the break-glass
public key present while its private half remains off the Controller?}

\section{Acceptance record}
Do not call the Controller rebuilt until this record is complete. Attach command
output rather than transcribing favorable fragments.

\begin{tabularx}{\linewidth}{@{}p{0.25\linewidth}p{0.27\linewidth}L@{}}
\toprule
\textbf{Evidence} & \textbf{Record} & \textbf{Pass condition}\\
\midrule
Build identity & commit; image filename; SHA-256 & Commit reviewed; digest
matches manifest.\\
Target identity & model; serial; size; interface MAC & Physical label, live
inventory and authorization summary agree.\\
Network plan & subnet; address; pool; client lease & Address outside pool;
lease inside; no default route.\\
Activation & first-boot journal; service states & Preflight passed and both
services active, or neither active.\\
Artifacts & boot URL; observed SHA-256 & Fetch succeeds; digest matches.\\
Convergence & first-run recap; second check recap & Intended changes applied;
final changed count is zero.\\
Recovery & recovery UKI date; custody check & Non-destructive boot
proved; off-host material located without exposing it.\\
\bottomrule
\end{tabularx}

\begin{tabularx}{\linewidth}{@{}p{0.18\linewidth}Xp{0.12\linewidth}X@{}}
Operator: & \hrulefill & Date/time: & \hrulefill\\[1.4em]
Witness/reviewer: & \hrulefill & Record ID: & \hrulefill
\end{tabularx}

\smallskip
\noindent\textbf{Attachments retained with this record}\par\smallskip
\begin{tabularx}{\linewidth}{@{}p{0.28\linewidth}Xp{0.25\linewidth}@{}}
\toprule
\textbf{Attachment} & \textbf{Filename or record reference} &
\textbf{Observed digest / result}\\
\midrule
Installer manifest and image & &\\[0.75em]
Authorization summary and disk inventory & &\\[0.75em]
First-boot journal and client proof & &\\[0.75em]
Apply recap and final check recap & &\\
\bottomrule
\end{tabularx}

\section{Recovering instead of rebuilding}
\begin{telosfacts}[0.24]
\telosfact{Bad update}{Boot the recovery UKI, restore the operating-system checkpoint and its matching ESP artifact set, reboot. The checkpoint is taken before the transaction, not after. \governedby{ADR 0028}}
\telosfact{Boot broken}{The recovery UKI is self-contained and boots without the installed root. It starts non-destructive and requires an explicit action before mounting anything writable. \governedby{ADR 0024}}
\telosfact{TPM unlock fails}{Milestone B only. Early boot falls back to the recovery key, which is held off-host. Test that path \emph{before} enabling TPM unlock, never after. \governedby{ADR 0029}}
\telosfact{Disk dead}{Rebuild from this manual. Application data restores from its own backup, on its own schedule --- it is not in the operating-system checkpoint. \governedby{ADR 0027, ADR 0054}}
\telosfact{Recovery key lost}{The data is gone. This is the intended behaviour of encryption at rest, and it is why custody is a decision with its own ADRs rather than an afterthought.}
\end{telosfacts}

\begin{telospanel}{Test this manual by using it}
The QEMU acceptance matrix runs this entire sequence unattended against virtual
hardware and asserts every expectation above. Any change to the installer, the
profile or these instructions must pass it before touching real metal.
A reconstruction manual nobody has followed end to end is a hypothesis.
\governedby{ADR 0056}
\end{telospanel}
\end{document}
