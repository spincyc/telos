\input{common/preamble.tex}
\input{homelab/shared/homelab.tex}

\telosmeta{Homelab}{Provisioning Design}
  {Network boot, the authorization boundary, and how it is tested}{2026-07-26}

\begin{document}
\telosmasthead[High-level design]

A machine is installed by network booting it into an interactive environment,
answering questions at its console, and typing the target disk's serial number
to authorize the erase. There is no pre-staged record of what a machine should
become, no answer file, and no unattended mode. Physical access to the console
plus that typed confirmation \emph{is} the authorization model.

\section{The boot chain}
\begin{center}
\begin{tikzpicture}[x=1cm,y=1cm,
  pencil/.style={draw=telosink,line width=0.58pt,line cap=round,line join=round},
  faint/.style={draw=telosmute,line width=0.28pt,line cap=round},
  signal/.style={-{Stealth[length=1.8mm,width=1.2mm]},draw=telosink,
    line width=0.62pt,double=telospaper,double distance=0.65pt}]
  % A measured baseline and registration ticks make the plate read as a
  % technical field sketch rather than a row of generic application boxes.
  \draw[faint] (0,-0.93) -- (15.35,-0.93);
  \foreach \x in {0.25,3.95,7.15,10.25,13.45,15.10}
    \draw[faint] (\x,-0.86)--(\x,-1.00);

  % Target motherboard / UEFI ROM.
  \begin{scope}[shift={(0.25,0)}]
    \draw[pencil,rounded corners=1pt] (0,-0.58) rectangle (2.25,0.78);
    \draw[faint] (0.08,-0.49) rectangle (2.17,0.69);
    \draw[pencil] (0.28,-0.28) rectangle (1.10,0.43);
    \foreach \y in {-0.17,0.00,0.17,0.34}
      \draw[faint] (0.18,\y)--(0.28,\y) (1.10,\y)--(1.21,\y);
    \draw[pencil] (1.48,0.09) circle (0.29);
    \draw[faint] (1.27,0.09)--(1.69,0.09) (1.48,-0.12)--(1.48,0.30);
    \draw[pencil] (1.35,-0.40) rectangle (1.98,-0.23);
    \node[font=\fontsize{5.7}{6.2}\selectfont] at (0.69,0.08) {UEFI};
  \end{scope}

  % Bootstrap host: stacked network appliance, with the active DHCP service.
  \begin{scope}[shift={(3.95,0)}]
    \foreach \y in {-0.52,-0.10,0.32}
      {\draw[pencil,rounded corners=1pt] (0,\y) rectangle (2.15,\y+0.34);
       \draw[faint] (0.12,\y+0.11)--(1.38,\y+0.11);
       \fill (1.71,\y+0.17) circle (0.035);
       \draw[pencil] (1.86,\y+0.10) rectangle (2.02,\y+0.24);}
    \draw[faint] (0.20,-0.62)--(0.20,-0.52) (1.95,-0.62)--(1.95,-0.52);
  \end{scope}

  % The deliberately tiny TFTP payload: one ROM-sized loader.
  \begin{scope}[shift={(7.15,0)}]
    \draw[pencil] (0.18,-0.42) rectangle (1.72,0.58);
    \draw[faint] (0.28,-0.32) rectangle (1.62,0.48);
    \foreach \x in {0.36,0.65,0.94,1.23,1.52}
      \draw[pencil] (\x,-0.52)--(\x,-0.42) (\x,0.58)--(\x,0.68);
    \node[font=\fontsize{6.1}{6.4}\selectfont\ttfamily] at (0.95,0.16) {iPXE};
    \draw[faint] (0.46,-0.08)--(1.44,-0.08);
  \end{scope}

  % HTTP artifact and its manifest: the integrity relationship is visible.
  \begin{scope}[shift={(10.25,0)}]
    \draw[pencil] (0.08,-0.55)--(0.08,0.70)--(1.44,0.70)--(1.82,0.32)--(1.82,-0.55)--cycle;
    \draw[pencil] (1.44,0.70)--(1.44,0.32)--(1.82,0.32);
    \foreach \y in {0.13,-0.05,-0.23}
      \draw[faint] (0.32,\y)--(1.52,\y);
    \draw[pencil] (1.24,-0.38) circle (0.26);
    \draw[pencil] (1.12,-0.38)--(1.21,-0.29)--(1.38,-0.48);
  \end{scope}

  % The received installer environment, shown as a console on the target.
  \begin{scope}[shift={(13.45,0)}]
    \draw[pencil,rounded corners=1pt] (0,-0.43) rectangle (1.90,0.71);
    \draw[faint] (0.12,-0.31) rectangle (1.78,0.58);
    \draw[pencil] (0.28,0.35)--(0.47,0.20)--(0.28,0.05);
    \draw[pencil] (0.56,0.04)--(1.25,0.04);
    \draw[pencil] (0.69,-0.43)--(0.58,-0.68)--(1.32,-0.68)--(1.21,-0.43);
    \draw[pencil] (0.40,-0.68)--(1.50,-0.68);
  \end{scope}

  \foreach \a/\b in {2.50/3.95,6.10/7.33,8.87/10.33,12.07/13.45}
    \draw[signal] (\a,0.04)--(\b,0.04);

  \node[font=\small\bfseries,align=center] at (1.38,-1.22) {target firmware};
  \node[font=\small\bfseries,align=center] at (5.03,-1.22) {dnsmasq};
  \node[font=\small\bfseries,align=center] at (8.10,-1.22) {TFTP};
  \node[font=\small\bfseries,align=center] at (11.20,-1.22) {nginx};
  \node[font=\small\bfseries,align=center] at (14.40,-1.22) {installer};
  \node[hlabel,align=center] at (5.03,-1.63) {DHCP + PXE\\one authority};
  \node[hlabel,align=center] at (8.10,-1.63) {iPXE only\\unauthenticated};
  \node[hlabel,align=center] at (11.20,-1.63) {artifacts + manifest\\SHA-256 checked};
  \node[hlabel,align=center] at (14.40,-1.63) {Archiso\\interactive};
\end{tikzpicture}
\end{center}

\begin{telosfacts}[0.20]
\telosfact{Why the split}{TFTP is unauthenticated and slow, so it carries only the first-stage loader. Kernels, initramfs images and the root filesystem travel over HTTP, where they can be checksummed against a published manifest. \governedby{ADR 0044, ADR 0048}}
\telosfact{One DHCP authority}{Never two on one segment, at any stage, including transitions. Where external infrastructure already owns DHCP, the bootstrap host runs in ProxyDHCP mode and supplies boot options only. \governedby{ADR 0008, ADR 0052}}
\telosfact{No menu}{The iPXE script boots exactly one thing. A menu with a single entry is a timeout waiting to select the wrong option.}
\telosfact{Checksums, not signatures}{During Milestone A artifact integrity comes from a published SHA-256 manifest that the installer verifies before use. Authenticated boot is Milestone B. \governedby{ADR 0043}}
\end{telosfacts}

\section{The installer}
\begin{tabularx}{\linewidth}{@{}p{0.05\linewidth}p{0.26\linewidth}L@{}}
\toprule
& \textbf{Stage} & \textbf{What happens}\\
\midrule
1 & Collect & Disks, interfaces and firmware read from the running machine behind a substitutable seam, so the judgement logic is testable without hardware.\\
2 & Refuse & Legacy BIOS stops the run outright. So does a machine with no eligible disk, or a Controller with no wired interface. \governedby{ADR 0019, ADR 0050}\\
3 & Ask & Only the questions that apply. Validators see the answers already given, so a rule is reported at the prompt that broke it.\\
4 & Validate & The network plan is checked again as a whole before the summary. \governedby{ADR 0045}\\
5 & Summarise & Profile, hostname, firmware, interface, the entered and derived network plan, the exact disk, and the layout to be written. \governedby{ADR 0004}\\
6 & Authorize & The operator types the target disk's \textbf{serial number}. Not \texttt{yes}. \governedby{ADR 0058}\\
7 & Execute & Steps run in order and stop at the first failure. Nothing is rolled back. \governedby{ADR 0059}\\
8 & Record & A non-secret manifest is written to the new root and echoed to the console. \governedby{ADR 0060}\\
9 & Power off & Never reboot. Relocation while powered off is the DHCP-conflict boundary. \governedby{ADR 0010}\\
\bottomrule
\end{tabularx}

\newpage
\section{Interactive state machine}
\begin{center}
\begin{tikzpicture}[
  node distance=7mm and 7mm,
  s/.style={draw,rounded corners=1pt,minimum width=2.25cm,
    minimum height=.82cm,align=center,font=\scriptsize},
  flow/.style={-{Stealth[length=1.6mm]},line width=.6pt},
  fail/.style={flow,dashed},
  lab/.style={font=\scriptsize,fill=white,inner sep=1pt}]
\node[s] (collect) {collect facts\\read-only};
\node[s,right=of collect] (eligible) {refuse ineligible\\targets};
\node[s,right=of eligible] (ask) {ask applicable\\questions};
\node[s,right=of ask] (valid) {validate whole\\plan};
\node[s,right=of valid] (summary) {freeze summary\\and step plan};
\node[s,below=10mm of summary] (serial) {match full\\typed serial};
\node[s,left=of serial] (execute) {execute once\\stop on failure};
\node[s,left=of execute] (record) {write manifest\\power off};
\node[s,below=10mm of eligible] (refuse) {refuse or cancel\\without writes};
\node[s,below=10mm of execute] (failed) {stop immediately\\preserve evidence};
\draw[flow] (collect)--(eligible);
\draw[flow] (eligible)--(ask);
\draw[fail] (eligible)--node[lab,left]{no eligible disk}(refuse);
\draw[flow] (ask)--(valid);
\draw[fail] (valid) to[bend left=35] node[lab,above]{re-ask cause} (ask);
\draw[flow] (valid)--(summary);
\draw[flow] (summary)--(serial);
\draw[fail] (serial.east) -- ++(.65,0) |- node[lab,right]{mismatch} (refuse.east);
\draw[flow] (serial)--(execute);
\draw[flow] (execute)--(record);
\draw[fail] (execute)--node[lab,right]{error}(failed);
\end{tikzpicture}
\end{center}

The authorization gate occurs after discovery and validation but before the
first write. No answer gathered earlier is authorization. After execution
starts, the program never loops back into questioning and never attempts an
invented rollback.

\section{Operator dialogue}
\begin{tabularx}{\linewidth}{@{}p{.19\linewidth}p{.36\linewidth}L@{}}
\toprule
\textbf{Moment} & \textbf{Question shown or asked aloud} & \textbf{Evidence before continuing}\\
\midrule
Discovery & ``Do chassis identity, UEFI mode, disk serials and permanent MACs describe the machine in front of you?'' & Hardware fact report saved before any prompt; removable and ineligible devices listed separately with reasons.\\
Profile & ``Controller or Workstation --- which boundary will this machine own?'' & Resulting required questions previewed; Controller requires one wired permanent MAC and a four-input network plan.\\
Disk & ``Which eligible serial is the intended target, and where is its backup proof?'' & Model, serial, size, bus and existing signatures displayed together; operator identifies restore source if any data is expected.\\
Network & ``Can this subnet, pool and Controller address coexist without a router or a second authority?'' & Derived network/broadcast/netmask shown; address outside pool; pool excludes network and broadcast; no gateway field exists.\\
Summary & ``If this exact summary were printed after failure, would it identify every intended write?'' & Partition table, mount layout, host identity, interface binding and execution steps frozen to a digest.\\
Authorization & ``Type the full target serial to erase only this disk.'' & Exact, case-sensitive match to the frozen disk identity; paste is accepted only as text, never as a generic confirmation.\\
Completion & ``Does the on-disk manifest match the frozen summary and console copy?'' & Read-back from mounted target, digest comparison, clean unmount and explicit poweroff event.\\
\bottomrule
\end{tabularx}

\begin{telospanel}{Questions are controls}
Each prompt should eliminate one class of ambiguity and expose the evidence
needed to answer it. If an operator must remember an unstated fact, consult a
second screen, or interpret a bare device name such as \texttt{/dev/sda}, the
prompt has failed even when its validator accepts the input.
\end{telospanel}

\section{Execution ledger and evidence}
\begin{tabularx}{\linewidth}{@{}p{.18\linewidth}p{.27\linewidth}L@{}}
\toprule
\textbf{Step} & \textbf{Precondition} & \textbf{Recorded proof}\\
\midrule
Erase and partition & Serial still resolves to the same whole-disk device; frozen size and identity unchanged & Before/after partition-table dumps; command, exit status, monotonic timestamps.\\
Encrypt & Target partitions match the frozen plan; secrets absent from logs & LUKS2 metadata summary and UUID, key-source class, successful unlock test; never the passphrase.\\
Filesystems & Mapped device and expected UUIDs are unique & Filesystem labels, UUIDs, Btrfs subvolume inventory and mount options.\\
Base install & Artifact hashes equal the served manifest & Manifest digest, package database snapshot, repository snapshot date and failed-package output if any.\\
Boot artifacts & ESP mounted from the selected disk; kernel/initramfs inputs complete & Boot-entry listing, UKI hashes, ESP free space and firmware-entry read-back.\\
Network and SSH & Permanent MAC and administrator public-key fingerprint frozen & Generated unit/config digests; offline syntax checks; no private key or DHCP service activation.\\
Manifest & All prior successful ledger entries available & Read-back from new root equals console copy; mode is \texttt{development-proof}; secrets explicitly excluded.\\
Shutdown & Filesystems synced and unmounted & Unmount ledger, final poweroff request and absence of an automatic reboot path.\\
\bottomrule
\end{tabularx}

\section{Failure branches}
\begin{tabularx}{\linewidth}{@{}p{.21\linewidth}p{.28\linewidth}L@{}}
\toprule
\textbf{Failure point} & \textbf{Immediate action} & \textbf{Recovery rule}\\
\midrule
Before serial gate & Refuse or cancel; make no writes & Correct the answer or hardware and begin a new run. Preserve the rejected fact report when diagnosing discovery.\\
Identity changes at gate & Cancel even if the typed serial matched the earlier display & Rescan from zero. A device path, capacity or serial change invalidates the frozen summary.\\
Partition/encryption & Stop; do not format further partitions & Treat target contents as indeterminate. Export ledger, power off, inspect from separate rescue media, then restart a full install.\\
Artifact mismatch & Stop before using the artifact & Quarantine artifact and manifest together. Repair publication; never ``update the checksum'' without reproducing the artifact.\\
Package or boot failure & Stop; leave later configuration undone & Retain logs and mounted target for read-only inspection. Reinstallation, not ad hoc continuation, is the supported recovery.\\
Manifest read-back mismatch & Do not declare completion or relocate & Preserve both copies, unmount, power off and investigate from rescue media. The install is failed even if it boots.\\
Power loss & Assume the active step is incomplete & Boot rescue media, collect read-only storage facts and start again. There is no resume token and no inferred rollback.\\
\bottomrule
\end{tabularx}

\section{What cannot be offered}
Some things are excluded from the choices rather than merely warned about,
because a warning is something an operator can read past at one in the morning.

\begin{telosfacts}[0.24]
\telosfact{Removable disks}{The installer boots from removable media. Offering the stick it is running from is the most destructive available mistake, so it never appears in the list --- with the reason printed, so the operator can see it was considered and rejected.}
\telosfact{Disks with no serial}{ADR 0058 confirms the erase by typing the serial. A disk that reports none cannot be confirmed, so it must not be offered. That rule fell out of the confirmation design rather than being invented separately.}
\telosfact{Disks under 64 GiB}{Not a computed minimum. A floor below which the operator has almost certainly selected the wrong device. \governedby{ADR 0051}}
\telosfact{Wireless interfaces}{Bare-metal network installation is wired, and a Controller serving DHCP over wireless is not a supported design.}
\telosfact{An unplugged wired interface}{\emph{Is} offered. The Controller is routinely provisioned on one network and relocated while powered off, so the managed interface is often disconnected during installation. Carrier is noted here and required at first boot. \governedby{ADR 0010, ADR 0009}}
\end{telosfacts}

\section{How it is tested}
The acceptance harness attaches to a pseudo-terminal and answers the genuine
prompts the way a person would, including typing the serial. There is no
unattended code path for it to use, so there is nothing to abuse on real
hardware, and the program under test is byte-identical to the one that runs on
metal.

\begin{telosfacts}[0.20]
\telosfact{One source of truth}{The installer renders a prompt registry; the harness reads the same registry to know what to answer. A reworded prompt cannot desynchronize them. \governedby{ADR 0058}}
\telosfact{The harness refuses to guess}{A prompt that appears and is not in the script is an error. An authorization prompt with no scripted confirmation is an error. Guessing is how a harness ends up confirming an erase nobody scripted.}
\telosfact{Same driver, two targets}{Locally it drives the installer as a subprocess. In the lab it drives a QEMU guest whose serial console is the process's stdio. The installer cannot tell the difference. \governedby{ADR 0056}}
\telosfact{The topology enforces the boundary}{The virtual lab has no user networking and no default NIC --- only a socket segment between guests. ADR 0011's no-routing rule is a property of the lab, not a setting in it.}
\telosfact{Fresh firmware every run}{Each guest gets its own OVMF variable store, so boot entries from one run cannot make a later run pass.}
\end{telosfacts}

\section{Acceptance matrix}
\begin{tabularx}{\linewidth}{@{}p{.20\linewidth}p{.33\linewidth}L@{}}
\toprule
\textbf{Claim} & \textbf{Measurable acceptance} & \textbf{Retained proof}\\
\midrule
No unattended path & Every run encounters the serial prompt; EOF, timeout, empty input, unknown prompt and wrong serial all exit before the first write & PTY transcript plus write-driver call count of zero for each negative case.\\
Correct exclusion & Removable, serial-less and sub-64-GiB disks are never selectable across the device-fixture matrix & Fact fixture, displayed rejection reason and eligible-list snapshot.\\
Prompt relevance & Controller-only questions never appear for a Workstation; every unexpected prompt fails the harness & Prompt-registry coverage report and transcript for both profiles.\\
Validation locality & Each invalid network answer is rejected at the prompt that made the plan invalid & Boundary-value matrix covering network/broadcast, pool order, overlap and Controller-in-pool cases.\\
Stop on first error & For an injected failure at every execution step, no later driver method is called & Ordered call ledger for each fault injection, with the first error retained verbatim.\\
Manifest fidelity & On-disk and console manifests are byte-identical and represent the frozen summary & Digest and semantic comparison after a successful QEMU install.\\
Bootability & Fresh OVMF state boots both installed kernels and rescue artifact from the target disk & Serial transcript, booted kernel identity and storage UUID assertions.\\
Network isolation & QEMU guests have no default NIC, user networking or host route & QEMU argv, socket-topology inventory and guest route table.\\
Repeatability & Three clean lab runs from new disks and new OVMF variables produce equal semantic manifests & Run IDs, artifact hashes and explained differences limited to declared UUID/time fields.\\
\bottomrule
\end{tabularx}

\section{End-to-end proof sequence}
\begin{enumerate}
\item Build artifacts once and freeze their manifest digest. Record the source
revision and tool versions.
\item Run negative PTY cases first: unknown prompt, wrong serial, EOF and every
validator boundary. Assert zero write calls.
\item Run the same dialogue in QEMU on a fresh virtual disk and OVMF variable
store. Inject one failure at each write step and prove that the ledger ends
there.
\item Complete a successful Controller install. Before booting it, mount the
target read-only and compare its manifest, layout and boot artifacts with the
frozen summary.
\item Boot the installed disk with no bootstrap host dependency. Record kernel,
root UUID, subvolumes, mode and interface binding; verify DHCP remains stopped.
\item Power off, connect the virtual Controller to the isolated socket segment,
then perform the separate first-boot activation tests. Provisioning acceptance
ends at poweroff; network-authority acceptance belongs to the Controller
runbook.
\end{enumerate}

\begin{gate}{Final proof must survive review}
The release bundle contains source revision, artifact and prompt-registry
digests, hardware fixtures, transcripts, ordered driver ledgers, injected-fault
results, QEMU topology, fresh-firmware boot evidence, manifests and all
explained differences. A successful screenshot or a booting machine alone is
not evidence that the erase boundary, isolation boundary or stop-on-error rule
worked.
\end{gate}

\begin{telospanel}{A bug this found}
The rule that matters most is that the Controller's address must never sit
inside the DHCP pool --- dnsmasq must not be able to lease the address its own
DNS answers on. Asked in ADR 0045's order, that violation surfaced two prompts
late, while the operator was being asked for the pool's last address and told
that a different field was wrong. The questions are therefore asked subnet,
pool, Controller address: last, so the error lands on the prompt that caused it.
Driving the real interface found that; no unit test would have.
\end{telospanel}

\begin{gate}{Milestone A is disposable by construction}
Every installation during the functional proof is labelled
\texttt{development-proof} in its manifest, requires the LUKS2 passphrase at
every boot, and carries no production data. That label is a recorded state, not
a remembered one, so later tooling can refuse to treat such a machine as
production. \governedby{ADR 0043, ADR 0060}
\end{gate}
\end{document}
