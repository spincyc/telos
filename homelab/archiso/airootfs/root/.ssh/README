Administrator PUBLIC keys only.

Place authorized_keys here to allow a parallel SSH session for observing an
installation. ADR 0049: no private key, token, passphrase or other credential
is ever placed in this image. The image is published with its checksum and is
readable by anyone who can fetch it.

This file is gitignored in the instance overlay; the key belongs to a person,
not to the profile.
