#!/usr/bin/env python3
"""Prepare one private serial-authorized Windows installation bundle."""

from pathlib import Path
import sys

sys.path.insert(0, str(Path(__file__).resolve().parents[2]))

from homelab.vm.windows_install_prepare import main  # noqa: E402


raise SystemExit(main())
