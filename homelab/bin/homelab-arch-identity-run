#!/usr/bin/env python3
"""Run one prepared, live Arch identity acceptance from a bundle."""

from pathlib import Path
import sys

sys.path.insert(0, str(Path(__file__).resolve().parents[2]))

from homelab.vm.arch_identity_run import main  # noqa: E402


raise SystemExit(main())
