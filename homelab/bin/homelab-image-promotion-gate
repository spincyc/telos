#!/usr/bin/env python3
"""Prove one candidate image root against its contract and signed seed."""

from pathlib import Path
import sys

sys.path.insert(0, str(Path(__file__).resolve().parents[2]))

from homelab.lib.image_promotion_cli import main  # noqa: E402


raise SystemExit(main())
