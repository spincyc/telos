# Managed by Ansible (roles/identity_client). Edits are overwritten.
[libdefaults]
    default_realm = {{ homelab_identity_realm | upper }}
    dns_lookup_realm = false
    dns_lookup_kdc = true
    rdns = false
    ticket_lifetime = 24h
    renew_lifetime = 7d
    forwardable = true
