# GENERATED by Ansible. The AD DC signs time replies for domain clients.
driftfile /var/lib/ntp/ntp.drift
restrict default kod limited nomodify nopeer noquery
restrict 127.0.0.1
restrict ::1
{% for upstream in homelab_ad_ntp_upstreams %}
server {{ upstream }} iburst
{% endfor %}
ntpsigndsocket {{ homelab_ad_private_dir }}/ntp_signd
